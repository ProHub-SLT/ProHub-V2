@{
    ViewData["Title"] = "Employee Login";
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - PROHUB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1a2a3a 0%, #2c3e50 30%, #3498db 70%, #1a2a3a 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            position: relative;
            overflow-x: hidden;
            color: white;
        }

        /* === ENHANCED ANIMATED BACKGROUND === */
        #animated-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            overflow: hidden;
        }

        .floating-shape {
            position: absolute;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(0,182,241,0.15) 0%, rgba(57,181,74,0.05) 70%, transparent 100%);
            filter: blur(15px);
            animation: float 20s infinite ease-in-out;
        }

        .floating-shape:nth-child(1) {
            width: 300px;
            height: 300px;
            top: 10%;
            left: 5%;
            animation-delay: 0s;
            background: radial-gradient(circle, rgba(0,182,241,0.2) 0%, rgba(57,181,74,0.1) 70%, transparent 100%);
        }

        .floating-shape:nth-child(2) {
            width: 200px;
            height: 200px;
            top: 60%;
            left: 80%;
            animation-delay: -5s;
            background: radial-gradient(circle, rgba(57,181,74,0.2) 0%, rgba(0,182,241,0.1) 70%, transparent 100%);
        }

        .floating-shape:nth-child(3) {
            width: 400px;
            height: 400px;
            top: 30%;
            left: 70%;
            animation-delay: -10s;
            background: radial-gradient(circle, rgba(0,182,241,0.15) 0%, rgba(57,181,74,0.05) 70%, transparent 100%);
        }

        .floating-shape:nth-child(4) {
            width: 250px;
            height: 250px;
            top: 70%;
            left: 10%;
            animation-delay: -15s;
            background: radial-gradient(circle, rgba(57,181,74,0.15) 0%, rgba(0,182,241,0.05) 70%, transparent 100%);
        }

        @@keyframes float {
            0%, 100% {
                transform: translate(0, 0) rotate(0deg);
            }
            25% {
                transform: translate(20px, 30px) rotate(5deg);
            }
            50% {
                transform: translate(-15px, 20px) rotate(-5deg);
            }
            75% {
                transform: translate(10px, -15px) rotate(3deg);
            }
        }

        .gradient-mesh {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(57, 181, 74, 0.1) 0%, transparent 40%),
                radial-gradient(circle at 80% 70%, rgba(0, 182, 241, 0.1) 0%, transparent 40%),
                radial-gradient(circle at 40% 80%, rgba(57, 181, 74, 0.1) 0%, transparent 40%),
                radial-gradient(circle at 60% 20%, rgba(0, 182, 241, 0.1) 0%, transparent 40%);
            animation: meshMove 30s infinite alternate ease-in-out;
        }

        @@keyframes meshMove {
            0% {
                transform: scale(1) rotate(0deg);
            }
            100% {
                transform: scale(1.1) rotate(1deg);
            }
        }

        .particles-container {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }

        .container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 80px;
            max-width: 1300px;
            width: 100%;
            align-items: center;
            position: relative;
            z-index: 1;
        }
        .left-section {
            color: white;
            padding: 40px;
            animation: slideInLeft 0.8s ease-out;
        }
        @@keyframes slideInLeft {
            from { opacity: 0; transform: translateX(-50px); }
            to { opacity: 1; transform: translateX(0); }
        }
        .logo-container {
            display: flex;
            align-items: center;
            margin-bottom: 60px;
            animation: fadeIn 1s ease-out;
        }
        @@keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        .logo {
            width: 140px;
            height: 140px;
            background: rgba(255, 255, 255, 0.08);
            border: 3px solid rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            backdrop-filter: blur(20px);
            box-shadow: 0 15px 50px rgba(0, 0, 0, 0.3);
            position: relative;
            animation: pulse 3s infinite ease-in-out;
            margin-right: 25px;
            flex-shrink: 0;
        }
        @@keyframes pulse {
            0%, 100% { box-shadow: 0 15px 50px rgba(0,0,0,0.3), 0 0 0 0 rgba(52,152,219,0.4); }
            50% { box-shadow: 0 15px 60px rgba(0,0,0,0.4), 0 0 0 20px rgba(52,152,219,0); }
        }
        .logo img { 
            width: 75px; 
            height: 75px;
            object-fit: contain;
        }
        .prohub-brand {
            font-size: 3.5rem;
            font-weight: 700;
            background: linear-gradient(135deg, #00B6F1 0%, #39B54A 100%);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            line-height: 1;
            margin-bottom: 5px;
            letter-spacing: -1px;
        }
        .prohub-tagline {
            font-size: 0.95rem;
            color: rgba(255,255,255,0.75);
            letter-spacing: 3px;
            text-transform: uppercase;
            font-weight: 500;
        }
        .main-title {
            font-size: 3.8rem;
            font-weight: 400;
            margin-bottom: 20px;
            background: linear-gradient(90deg, #39B54A, #00B6F1, #39B54A);
            background-size: 200% auto;
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            animation: gradient 4s ease infinite;
            line-height: 1.2;
        }
        @@keyframes gradient {
            0%, 100% { background-position: 0% center; }
            50% { background-position: 100% center; }
        }
        .subtitle {
            font-size: 1.3rem;
            color: rgba(255,255,255,0.85);
            line-height: 1.7;
            animation: fadeInUp 1s ease-out 0.3s both;
        }
        @@keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .decorative-line {
            width: 120px;
            height: 4px;
            background: linear-gradient(90deg, #39B54A, #00B6F1);
            margin: 40px 0;
            border-radius: 3px;
            animation: expandLine 1s ease-out 0.5s both;
        }
        @@keyframes expandLine {
            from { width: 0; }
            to { width: 120px; }
        }

        .login-card {
            background: rgba(255, 255, 255, 0.98);
            border-radius: 30px;
            padding: 0;
            box-shadow: 0 25px 70px rgba(0, 0, 0, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.2);
            overflow: hidden;
            animation: slideInRight 0.8s ease-out;
            position: relative;
            max-width: 550px;
            margin: 0 auto;
            width: 100%;
        }
        @@keyframes slideInRight {
            from { opacity: 0; transform: translateX(50px); }
            to { opacity: 1; transform: translateX(0); }
        }
        .login-card::before {
            content: '';
            position: absolute;
            top: 0; left: -100%;
            width: 100%; height: 100%;
            background: linear-gradient(90deg, transparent, rgba(52,152,219,0.1), transparent);
            animation: shimmer 3s infinite;
        }
        @@keyframes shimmer {
            0% { left: -100%; }
            100% { left: 100%; }
        }
        .login-header {
            background: linear-gradient(135deg, #2c3e50, #3498db);
            padding: 50px 40px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        .login-header::before {
            content: '';
            position: absolute;
            top: -50%; right: -50%;
            width: 200%; height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1), transparent);
            animation: rotate 10s linear infinite;
        }
        @@keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        .login-header h2 {
            font-size: 2.5rem;
            font-weight: 600;
            color: white;
            margin-bottom: 12px;
            position: relative;
            z-index: 1;
            text-shadow: 0 2px 10px rgba(0,0,0,0.2);
        }
        .login-header p {
            color: rgba(255,255,255,0.95);
            font-size: 1.1rem;
            position: relative;
            z-index: 1;
        }
        .auth-section {
            padding: 50px 40px;
            text-align: center;
            background: url('/images/bg.jpg') no-repeat center center / cover;
            background-attachment: fixed;
            position: relative;
        }
        .auth-section::before {
            content: '';
            position: absolute;
            inset: 0;
            background: rgba(255,255,255,0.3);
            z-index: 0;
        }
        .auth-section > * { position: relative; z-index: 1; }
        .auth-icon {
            width: 95px;
            height: 95px;
            background: linear-gradient(135deg, #3498db, #2c3e50);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 35px;
            box-shadow: 0 15px 40px rgba(52,152,219,0.4);
            animation: bounce 2s infinite;
        }
        @@keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }
        .auth-icon::after {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            border: 2px solid #3498db;
            animation: ripple 2s infinite;
        }
        @@keyframes ripple {
            0% { transform: scale(1); opacity: 1; }
            100% { transform: scale(1.5); opacity: 0; }
        }
        .auth-icon i { font-size: 2.8rem; color: white; z-index: 1; }
        .auth-title { 
            font-size: 1.8rem; 
            font-weight: 600; 
            color: #2c3e50; 
            margin-bottom: 15px;
        }
        .auth-subtitle { 
            color: #5a6c7d; 
            margin-bottom: 40px; 
            font-size: 1.05rem;
        }
        .azure-btn {
            background: linear-gradient(135deg, #2c3e50 0%, #3498db 100%);
            color: white;
            border: none;
            border-radius: 15px;
            padding: 22px 55px;
            font-size: 1.15rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.4s ease;
            display: inline-flex;
            align-items: center;
            gap: 15px;
            box-shadow: 0 8px 25px rgba(52,152,219,0.3);
            position: relative;
            overflow: hidden;
            text-decoration: none;
        }
        .azure-btn::before {
            content: '';
            position: absolute;
            top: 50%; left: 50%;
            width: 0; height: 0;
            border-radius: 50%;
            background: linear-gradient(135deg, #39B54A, #00B6F1);
            transform: translate(-50%, -50%);
            transition: width 0.6s ease, height 0.6s ease;
            z-index: 0;
        }
        .azure-btn:hover::before {
            width: 300%;
            height: 300%;
        }
        .azure-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(52,152,219,0.5);
        }
        .azure-btn i { 
            font-size: 1.6rem; 
            transition: transform 0.3s ease;
            position: relative;
            z-index: 1;
        }
        .azure-btn span {
            position: relative;
            z-index: 1;
        }
        .azure-btn:hover i { transform: rotate(360deg); }
        .support-link {
            margin-top: 40px;
            color: #5a6c7d;
            font-size: 0.98rem;
        }
        .support-link a {
            color: #3498db;
            text-decoration: none;
            font-weight: 600;
            position: relative;
        }
        .support-link a::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 0;
            height: 2px;
            background: #3498db;
            transition: width 0.3s ease;
        }
        .support-link a:hover::after { width: 100%; }
        .support-link a:hover { color: #39B54A; }

        /* === RESPONSIVE DESIGN === */
        
        /* Tablets and smaller desktops */
        @@media (max-width: 1200px) {
            .container {
                gap: 60px;
                max-width: 1000px;
            }
            .logo {
                width: 120px;
                height: 120px;
                margin-right: 20px;
            }
            .logo img {
                width: 65px;
                height: 65px;
            }
            .prohub-brand {
                font-size: 3rem;
            }
            .main-title {
                font-size: 3.2rem;
            }
            .subtitle {
                font-size: 1.2rem;
            }
        }

        /* Tablets */
        @@media (max-width: 1024px) {
            .container { 
                grid-template-columns: 1fr; 
                gap: 50px;
                padding: 20px;
            }
            .left-section { 
                text-align: center; 
                padding: 20px;
            }
            .logo-container { 
                justify-content: center;
                margin-bottom: 40px;
            }
            .decorative-line { 
                margin: 30px auto;
            }
            .login-card {
                max-width: 600px;
            }
        }

        /* Small tablets and large phones */
        @@media (max-width: 768px) {
            body {
                padding: 15px;
            }
            .container {
                gap: 40px;
            }
            .left-section {
                padding: 15px;
            }
            .logo-container {
                margin-bottom: 30px;
                flex-direction: column;
                gap: 20px;
            }
            .logo {
                width: 100px;
                height: 100px;
                margin-right: 0;
            }
            .logo img {
                width: 55px;
                height: 55px;
            }
            .prohub-brand { 
                font-size: 2.5rem;
            }
            .prohub-tagline {
                font-size: 0.85rem;
                letter-spacing: 2px;
            }
            .main-title { 
                font-size: 2.8rem;
                margin-bottom: 15px;
            }
            .subtitle {
                font-size: 1.1rem;
            }
            .decorative-line {
                width: 100px;
                height: 3px;
                margin: 25px auto;
            }
            .login-header { 
                padding: 40px 30px;
            }
            .login-header h2 { 
                font-size: 2rem;
            }
            .login-header p {
                font-size: 1rem;
            }
            .auth-section { 
                padding: 40px 30px;
            }
            .auth-icon {
                width: 80px;
                height: 80px;
                margin-bottom: 30px;
            }
            .auth-icon i {
                font-size: 2.4rem;
            }
            .auth-title {
                font-size: 1.5rem;
            }
            .auth-subtitle {
                font-size: 1rem;
                margin-bottom: 35px;
            }
            .azure-btn { 
                padding: 18px 40px; 
                font-size: 1rem;
                gap: 12px;
            }
            .azure-btn i {
                font-size: 1.4rem;
            }
            .support-link {
                margin-top: 35px;
                font-size: 0.95rem;
            }
            /* Adjust floating shapes for tablets */
            .floating-shape:nth-child(1) {
                width: 200px;
                height: 200px;
            }
            .floating-shape:nth-child(3) {
                width: 250px;
                height: 250px;
            }
        }

        /* Mobile phones */
        @@media (max-width: 576px) {
            body {
                padding: 10px;
            }
            .container {
                gap: 30px;
            }
            .left-section {
                padding: 10px;
            }
            .logo-container {
                margin-bottom: 25px;
                gap: 15px;
            }
            .logo {
                width: 80px;
                height: 80px;
            }
            .logo img {
                width: 45px;
                height: 45px;
            }
            .prohub-brand {
                font-size: 2rem;
            }
            .prohub-tagline {
                font-size: 0.75rem;
                letter-spacing: 1.5px;
            }
            .main-title {
                font-size: 2.2rem;
                margin-bottom: 12px;
            }
            .subtitle {
                font-size: 1rem;
                line-height: 1.5;
            }
            .decorative-line {
                width: 80px;
                height: 3px;
                margin: 20px auto;
            }
            .login-card {
                border-radius: 20px;
            }
            .login-header {
                padding: 30px 20px;
            }
            .login-header h2 {
                font-size: 1.6rem;
                margin-bottom: 10px;
            }
            .login-header p {
                font-size: 0.95rem;
            }
            .auth-section {
                padding: 30px 20px;
            }
            .auth-icon {
                width: 70px;
                height: 70px;
                margin-bottom: 25px;
            }
            .auth-icon i {
                font-size: 2rem;
            }
            .auth-title {
                font-size: 1.3rem;
                margin-bottom: 12px;
            }
            .auth-subtitle {
                font-size: 0.95rem;
                margin-bottom: 30px;
            }
            .azure-btn {
                padding: 16px 30px;
                font-size: 0.95rem;
                gap: 10px;
                border-radius: 12px;
                width: 100%;
                justify-content: center;
            }
            .azure-btn i {
                font-size: 1.3rem;
            }
            .support-link {
                margin-top: 30px;
                font-size: 0.9rem;
            }
            /* Reduce floating shapes on mobile */
            .floating-shape:nth-child(1),
            .floating-shape:nth-child(3) {
                width: 150px;
                height: 150px;
            }
            .floating-shape:nth-child(2),
            .floating-shape:nth-child(4) {
                width: 120px;
                height: 120px;
            }
        }

        /* Extra small phones */
        @@media (max-width: 400px) {
            .prohub-brand {
                font-size: 1.8rem;
            }
            .main-title {
                font-size: 2rem;
            }
            .subtitle {
                font-size: 0.95rem;
            }
            .login-header h2 {
                font-size: 1.4rem;
            }
            .auth-title {
                font-size: 1.2rem;
            }
            .azure-btn {
                padding: 14px 25px;
                font-size: 0.9rem;
            }
        }

        /* Landscape mode adjustments for mobile */
        @@media (max-width: 896px) and (orientation: landscape) {
            body {
                padding: 10px;
            }
            .container {
                grid-template-columns: 1fr 1fr;
                gap: 30px;
                align-items: flex-start;
            }
            .left-section {
                padding: 15px;
            }
            .logo-container {
                margin-bottom: 20px;
                flex-direction: row;
                gap: 15px;
            }
            .logo {
                width: 70px;
                height: 70px;
                margin-right: 15px;
            }
            .logo img {
                width: 40px;
                height: 40px;
            }
            .prohub-brand {
                font-size: 1.8rem;
            }
            .prohub-tagline {
                font-size: 0.7rem;
            }
            .main-title {
                font-size: 1.8rem;
                margin-bottom: 10px;
            }
            .subtitle {
                font-size: 0.9rem;
            }
            .decorative-line {
                margin: 15px 0;
            }
            .login-header {
                padding: 25px 20px;
            }
            .login-header h2 {
                font-size: 1.4rem;
            }
            .login-header p {
                font-size: 0.9rem;
            }
            .auth-section {
                padding: 25px 20px;
            }
            .auth-icon {
                width: 60px;
                height: 60px;
                margin-bottom: 20px;
            }
            .auth-icon i {
                font-size: 1.8rem;
            }
            .auth-title {
                font-size: 1.1rem;
            }
            .auth-subtitle {
                font-size: 0.85rem;
                margin-bottom: 25px;
            }
            .azure-btn {
                padding: 12px 25px;
                font-size: 0.85rem;
            }
            .support-link {
                margin-top: 20px;
                font-size: 0.85rem;
            }
        }
    </style>
</head>
<body>

    <!-- ENHANCED ANIMATED BACKGROUND -->
    <div id="animated-bg">
        <div class="gradient-mesh"></div>
        <div class="floating-shape"></div>
        <div class="floating-shape"></div>
        <div class="floating-shape"></div>
        <div class="floating-shape"></div>
        <div class="particles-container" id="particles-container"></div>
    </div>

    <div class="container">
        <div class="left-section">
            <div class="logo-container">
                <div class="logo">
                    <img src="/images/SltLogo.png" alt="PROHUB Logo">
                </div>
                <div class="logo-text-section">
                    <div class="prohub-brand">PROHUB</div>
                    <div class="prohub-tagline">SLT Digital Platform</div>
                </div>
            </div>
            <h1 class="main-title">Employee Portal</h1>
            <div class="decorative-line"></div>
            <p class="subtitle">Comprehensive platform for project management,<br />task tracking, and team collaboration</p>
        </div>
        <div class="login-card">
            <div class="login-header">
                <h2>Employee Login</h2>
                <p>Access your dashboard and workspace</p>
            </div>
            <div class="auth-section">
                <div class="auth-icon">
                    <i class="bi bi-shield-lock"></i>
                </div>
                <h3 class="auth-title">Microsoft Account Authentication</h3>
                <p class="auth-subtitle">Use your organization Microsoft account</p>
                @if (User.Identity.IsAuthenticated)
                {
                    <a href="@Url.Action("Index", "Home")" class="azure-btn">
                        <i class="bi bi-speedometer2"></i>
                        <span>Go to Dashboard</span>
                    </a>
                }
                else
                {
                    <a asp-area="MicrosoftIdentity"
                       asp-controller="Account"
                       asp-action="SignIn"
                       class="azure-btn">
                        <i class="bi bi-microsoft"></i>
                        <span>Login with Microsoft</span>
                    </a>
                }
                <div class="support-link">
                    Having trouble? <a href="/support">Contact IT Support</a>
                </div>
            </div>
        </div>
    </div>

   
    <!-- Enhanced Particle System -->
    <script>
        // Enhanced particle system with floating geometric shapes
        const particlesContainer = document.getElementById('particles-container');
        const canvas = document.createElement('canvas');
        particlesContainer.appendChild(canvas);
        const ctx = canvas.getContext('2d');
        
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        const particlesArray = [];
        const connections = [];
        const mouse = { x: null, y: null, radius: 150 };

        window.addEventListener('mousemove', (e) => {
            mouse.x = e.x;
            mouse.y = e.y;
        });

        window.addEventListener('resize', () => {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
            init();
        });

        class Particle {
            constructor() {
                this.x = Math.random() * canvas.width;
                this.y = Math.random() * canvas.height;
                this.size = Math.random() * 2 + 0.5;
                this.speedX = Math.random() * 0.5 - 0.25;
                this.speedY = Math.random() * 0.5 - 0.25;
                this.color = Math.random() > 0.5 ? 
                    `rgba(0, 182, 241, ${Math.random() * 0.5 + 0.3})` : 
                    `rgba(57, 181, 74, ${Math.random() * 0.5 + 0.3})`;
                this.originalSize = this.size;
            }
            update() {
                this.x += this.speedX;
                this.y += this.speedY;
                
                // Bounce off walls
                if (this.x > canvas.width || this.x < 0) this.speedX *= -1;
                if (this.y > canvas.height || this.y < 0) this.speedY *= -1;
                
                // Mouse interaction
                const dx = mouse.x - this.x;
                const dy = mouse.y - this.y;
                const distance = Math.sqrt(dx * dx + dy * dy);
                
                if (distance < mouse.radius) {
                    this.size = this.originalSize * 2;
                    const force = (mouse.radius - distance) / mouse.radius;
                    const angle = Math.atan2(dy, dx);
                    this.x -= Math.cos(angle) * force * 2;
                    this.y -= Math.sin(angle) * force * 2;
                } else {
                    this.size = this.originalSize;
                }
            }
            draw() {
                ctx.fillStyle = this.color;
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fill();
                
                // Add glow effect
                ctx.shadowColor = this.color;
                ctx.shadowBlur = 10;
                ctx.fill();
                ctx.shadowBlur = 0;
            }
        }

        function init() {
            particlesArray.length = 0;
            const numberOfParticles = (canvas.width * canvas.height) / 12000;
            for (let i = 0; i < numberOfParticles; i++) {
                particlesArray.push(new Particle());
            }
        }

        function connectParticles() {
            const maxDistance = 120;
            
            for (let a = 0; a < particlesArray.length; a++) {
                for (let b = a; b < particlesArray.length; b++) {
                    const dx = particlesArray[a].x - particlesArray[b].x;
                    const dy = particlesArray[a].y - particlesArray[b].y;
                    const distance = Math.sqrt(dx * dx + dy * dy);

                    if (distance < maxDistance) {
                        const opacity = 1 - distance / maxDistance;
                        
                        // Create gradient for connection lines
                        const gradient = ctx.createLinearGradient(
                            particlesArray[a].x, particlesArray[a].y,
                            particlesArray[b].x, particlesArray[b].y
                        );
                        
                        gradient.addColorStop(0, particlesArray[a].color);
                        gradient.addColorStop(1, particlesArray[b].color);
                        
                        ctx.strokeStyle = gradient;
                        ctx.globalAlpha = opacity * 0.3;
                        ctx.lineWidth = 0.5;
                        ctx.beginPath();
                        ctx.moveTo(particlesArray[a].x, particlesArray[a].y);
                        ctx.lineTo(particlesArray[b].x, particlesArray[b].y);
                        ctx.stroke();
                        ctx.globalAlpha = 1;
                    }
                }
            }
        }

        function animate() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            
            particlesArray.forEach(p => {
                p.update();
                p.draw();
            });
            
            connectParticles();
            
            requestAnimationFrame(animate);
        }

        // Initialize and start animation
        init();
        animate();
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>